name: extradeep
description: Extra-deep preset for redesigns, core failures, and deeply uncertain work.

execution:
  model: gpt-5.4
  reasoning: xhigh

behavior:
  mode: fixed
  baseline_profile: extradeep
  default_route: extradeep

output:
  lean_output: false
  silent_success: false
  patch_first: true
