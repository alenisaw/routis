name: deep
description: Deep preset for debugging, investigation, and riskier work.

execution:
  model: gpt-5.4
  reasoning: high

behavior:
  mode: fixed
  baseline_profile: deep
  default_route: deep

output:
  lean_output: false
  silent_success: false
  patch_first: true
