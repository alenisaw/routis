name: default
description: Dynamic auto-selection preset for normal CLI usage.

execution:
  model: dynamic
  reasoning: dynamic

behavior:
  mode: dynamic
  baseline_profile: balanced
  default_route: balanced

output:
  lean_output: true
  silent_success: true
  patch_first: true
