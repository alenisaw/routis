name: balanced
description: Balanced preset for normal implementation and maintenance work.

execution:
  model: gpt-5.4
  reasoning: medium

behavior:
  mode: fixed
  baseline_profile: balanced
  default_route: balanced

output:
  lean_output: true
  silent_success: true
  patch_first: true
