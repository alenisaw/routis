name: cheap
description: Cheap preset for quick and low-risk work.

execution:
  model: gpt-5.4-mini
  reasoning: none

behavior:
  mode: fixed
  baseline_profile: cheap
  default_route: cheap

output:
  lean_output: true
  silent_success: true
  patch_first: true
